package html

object Tags {
  val html: Element.Normal = Element.Normal("html", Nil, Nil)

  val head: Element.Normal = Element.Normal("head", Nil, Nil)

  val body: Element.Normal = Element.Normal("body", Nil, Nil)

  val div: Element.Normal = Element.Normal("div", Nil, Nil)
}
